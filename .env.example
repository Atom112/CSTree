# CSTree — AI 功能环境变量
#
# 在 Cloudflare Pages 后台设置以下环境变量，构建时自动注入 API Key，
# 用户无需手动配置即可使用「问 AI」功能。
#
# 获取方式：
# 1. 访问 https://cloud.siliconflow.cn 注册账号
# 2. 进入「API 密钥」页面 → 创建 API Key
# 3. 复制 sk- 开头的密钥
#
# 设置后重新部署即可生效。
PUBLIC_SILICONFLOW_API_KEY=sk-your-key-here
